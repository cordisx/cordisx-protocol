{
  "case": "manifest",
  "value": {
    "$schema": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/plugin-manifest.v2.schema.json",
    "schemaVersion": 2,
    "id": "over-scoped-channel",
    "capabilities": [
      {
        "name": "channel.events.receive",
        "required": true,
        "reason": { "key": "receive", "fallback": "Receive events" },
        "scope": { "providers": ["codex"] }
      }
    ],
    "services": []
  }
}
