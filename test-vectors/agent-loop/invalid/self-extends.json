{
  "case": "definition",
  "value": {
    "$schema": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/agent-definition.v1.schema.json",
    "contract": "cordisx.agent-definition/v1",
    "schemaVersion": 1,
    "identity": { "agentId": "reviewer", "revision": "definition-1" },
    "extends": [{ "agentId": "reviewer", "revision": "definition-1" }],
    "inherit": { "promptSections": "append", "rules": "merge", "skills": "merge", "tools": "replace", "mcpServers": "replace", "runtimeDefaults": "merge" }
  }
}
