{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/agent-loop-bound-client.v2.schema.json",
  "title": "CordisX Host-injected Bound Agent Loop Client v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "contract", "schemaVersion", "injection", "operations", "lifetime", "subscription", "durableLedger"],
  "properties": {
    "$schema": { "const": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/agent-loop-bound-client.v2.schema.json" },
    "contract": { "const": "cordisx.bound-agent-loop-client/v2" },
    "schemaVersion": { "const": 2 },
    "injection": { "const": "host-bound" },
    "operations": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "uniqueItems": true,
      "items": { "enum": ["createOrBind", "send", "subscribe", "dispose"] }
    },
    "lifetime": { "const": "fiber-owned" },
    "subscription": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pages", "unsubscribe", "ownerDispose"],
      "properties": {
        "pages": { "const": "ordered" },
        "unsubscribe": { "const": "explicit" },
        "ownerDispose": { "const": "terminates" }
      }
    },
    "durableLedger": {
      "type": "object",
      "additionalProperties": false,
      "required": ["operationId", "scope", "providerAffinity", "survivesClientDispose", "payloadMatch", "retention"],
      "properties": {
        "operationId": { "const": "commandId" },
        "scope": { "const": "owner-provider" },
        "providerAffinity": { "const": "generation-fenced" },
        "survivesClientDispose": { "const": true },
        "payloadMatch": { "const": "structural-exact" },
        "retention": {
          "type": "object",
          "additionalProperties": false,
          "required": ["active", "recoveryDays"],
          "properties": {
            "active": { "const": "logical-task-lifetime" },
            "recoveryDays": { "const": 30 }
          }
        }
      }
    }
  }
}
