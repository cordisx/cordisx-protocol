{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/plugin-manifest.v1.schema.json",
  "title": "CordisX Runtime Plugin Manifest v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schemaVersion", "id", "capabilities"],
  "properties": {
    "$schema": {
      "const": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/plugin-manifest.v1.schema.json"
    },
    "schemaVersion": { "const": 1 },
    "id": { "$ref": "ui-common.v1.schema.json#/$defs/localId" },
    "name": { "type": "string", "minLength": 1, "maxLength": 200 },
    "capabilities": {
      "type": "array",
      "maxItems": 14,
      "items": { "$ref": "#/$defs/capabilityDeclaration" }
    }
  },
  "$defs": {
    "capabilityName": {
      "enum": [
        "models.read",
        "tasks.catalog.read",
        "tasks.content.read",
        "tasks.create",
        "tasks.control",
        "turns.submit",
        "turns.control",
        "agent.events.read",
        "agent.history.read",
        "agent.messages.append",
        "agent.steps.reject",
        "agent.messages.transform",
        "agent.prompt.section",
        "agent.prompt.context"
      ]
    },
    "providerId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-zA-Z0-9][a-zA-Z0-9._:/-]*$"
    },
    "platformSessionRef": {
      "$ref": "platform-session.v1.schema.json#/$defs/sessionRef"
    },
    "agentSessionId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512
    },
    "scope": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "providers": {
          "type": "array",
          "minItems": 1,
          "maxItems": 32,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/providerId" }
        },
        "cwdRoots": {
          "type": "array",
          "minItems": 1,
          "maxItems": 32,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 4096,
            "pattern": "^(?:/.*|[A-Za-z]:[\\\\/].*|\\\\\\\\.+)$"
          }
        },
        "sessions": {
          "type": "array",
          "minItems": 1,
          "maxItems": 100,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/platformSessionRef" }
        },
        "sessionIds": {
          "type": "array",
          "minItems": 1,
          "maxItems": 100,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/agentSessionId" }
        }
      }
    },
    "capabilityDeclaration": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "required", "reason", "scope"],
      "properties": {
        "name": { "$ref": "#/$defs/capabilityName" },
        "required": { "type": "boolean" },
        "reason": { "$ref": "ui-common.v1.schema.json#/$defs/localizedText" },
        "scope": { "$ref": "#/$defs/scope" }
      }
    }
  }
}
