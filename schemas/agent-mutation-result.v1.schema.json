{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/agent-mutation-result.v1.schema.json",
  "title": "CordisX Agent Message-less Mutation Result v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "contract", "schemaVersion", "operation", "status"],
  "properties": {
    "$schema": { "const": "https://raw.githubusercontent.com/cordisx/cordisx-protocol/main/schemas/agent-mutation-result.v1.schema.json" },
    "contract": { "const": "cordisx.agent-mutation-result/v1" },
    "schemaVersion": { "const": 1 },
    "operation": { "enum": ["cancel", "dispose"] },
    "mutationId": { "$ref": "session-common.v1.schema.json#/$defs/opaqueId" },
    "status": { "enum": ["accepted", "denied", "unavailable"] },
    "code": { "enum": ["permission-denied", "not-owner", "agent-replaced", "plugin-generation-replaced", "connection-replaced", "host-unavailable", "unsupported"] }
  },
  "allOf": [
    { "if": { "properties": { "status": { "const": "accepted" } }, "required": ["status"] }, "then": { "properties": { "code": false } } },
    { "if": { "properties": { "status": { "const": "denied" } }, "required": ["status"] }, "then": { "required": ["code"], "properties": { "code": { "enum": ["permission-denied", "not-owner"] } } } },
    { "if": { "properties": { "status": { "const": "unavailable" } }, "required": ["status"] }, "then": { "required": ["code"], "properties": { "code": { "enum": ["agent-replaced", "plugin-generation-replaced", "connection-replaced", "host-unavailable", "unsupported"] } } } }
  ]
}
