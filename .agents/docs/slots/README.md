# Semantic Slots

Host declarations, slot kinds, entry identity, ordering, priority shadowing, component contracts, and child-slot ownership belong here.

- `control-plane-v1.md` defines adapter-neutral compose, replace, overlay,
  proxy, hide-native, partial authorization, conflict selection, safe Host
  bindings, and nested suppression without granting DOM authority.
